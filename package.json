{
    "name": "cf_ai_edge_chat_agent",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "wrangler dev",
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "typescript": "^5.6.0",
        "wrangler": "^3.80.0"
    },
    "dependencies": {
        "agents": "^0.0.1"
    }
}
