name = "cf_ai_edge_chat_agent"
main = "src/agent.ts"
compatibility_date = "2025-11-18"

# Only add this if Wrangler later complains:
# account_id = "<YOUR_ACCOUNT_ID>"

[ai]
binding = "AI"

[durable_objects]
bindings = [
  { name = "CHAT_AGENT", class_name = "ChatAgentDO" }
]

[[migrations]]
tag = "v1"
new_sqlite_classes = ["ChatAgentDO"]
